[tools]
node = "22.16.0"

[tasks.dev]
description = "Start Astro dev server"
run = "npm run dev"

[tasks.build]
description = "Build the site"
run = "npm run build"

[tasks.preview]
description = "Preview the built site"
run = "npm run preview"
